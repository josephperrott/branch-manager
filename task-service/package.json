{
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && tsc -p tsconfig.json",
    "deploy": "yarn build && gcloud functions deploy handlePresubmitPrTask --trigger-topic presubmit-pr-task --runtime nodejs8",
    "serve": "yarn build && functions deploy handlePresubmitPrTask --trigger-topic presubmit-pr-task"
  },
  "dependencies": {
    "firebase-admin":"^6.3.0",
    "firebase-functions": "^2.1.0",
    "body-parser": "^1.18.3",
    "node-fetch": "^2.3.0",
    "express": "^4.16.4",
    "shelljs": "^0.8.3",
    "typescript": "^3.2.2"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^3.1.0",
    "@types/node": "^10.12.12",
    "@octokit/rest": "^16.1.0"
  }
}