{
  "scripts": {
    "build": "rm -rf dist/ && tsc -p tsconfig.json",
    "deploy": "yarn build && gcloud functions deploy githubEvents --trigger-http --runtime nodejs8",
    "serve": "yarn build && functions deploy githubEvents --trigger-http"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^0.22.2",
    "firebase-admin": "^6.3.0",
    "node-fetch": "^2.3.0",
    "js-yaml": "^3.12.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "shelljs": "^0.8.3"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/shelljs": "^0.8.0",
    "@types/express": "^4.16.0",
    "@google-cloud/nodejs-repo-tools": "^3.0.0",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  }
}
